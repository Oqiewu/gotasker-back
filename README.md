# GoTasker - REST API для управления задачами

Учебный проект на Go для изучения языка и best practices backend-разработки. Простой менеджер задач (TODO list) с REST API, Docker и Clean Architecture.

## Статус проекта

🚧 **В разработке** - Базовая структура проекта создана, идет реализация функционала.

**Текущая версия**: v0.1.0 (базовая инициализация)

## Возможности

- ✅ Базовая структура проекта с Clean Architecture
- ✅ Docker и docker-compose конфигурация
- ✅ Health check endpoint
- 🚧 REST API для управления задачами (в разработке)
- 🚧 Хранение данных в JSON (в разработке)
- 📋 PostgreSQL поддержка (запланировано)
- 📋 Аутентификация JWT (запланировано)
- 📋 Swagger документация (запланировано)

## Технологический стек

- **Язык**: Go 1.21+
- **Web Framework**: Gin
- **Хранилище**: JSON → PostgreSQL (миграция запланирована)
- **Контейнеризация**: Docker, Docker Compose
- **Архитектура**: Clean Architecture / Layered Architecture

## Структура проекта

```
gotasker-back/
├── src/
│   ├── cmd/                    # Точка входа приложения
│   │   └── main.go
│   └── internal/               # Внутренние пакеты
│       ├── models/             # Domain модели и DTO
│       ├── repository/         # Интерфейсы для работы с данными
│       ├── service/            # Бизнес-логика
│       ├── handlers/           # HTTP handlers
│       ├── middleware/         # HTTP middleware
│       ├── config/             # Конфигурация
│       └── pkg/                # Общие пакеты
│           └── logger/         # Логирование
├── migrations/                 # Миграции БД
├── tests/                      # Тесты
│   └── integration/
├── data/                       # JSON файлы (временное хранилище)
├── docs/                       # Swagger документация
├── docker-compose.yml
├── Dockerfile
├── Makefile
├── go.mod
├── README.md
├── CLAUDE.md                   # Инструкции для Claude Code
└── LEARNING_PATH.md            # Поэтапный план обучения
```

## Быстрый старт

### Требования

- **Go 1.21+** (для локальной разработки)
- **Docker** и **Docker Compose** (рекомендуется)
- **Make** (опционально, для удобства)

### Установка и запуск

#### Вариант 1: С Docker (рекомендуется)

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/yourusername/gotasker-back.git
cd gotasker-back

# 2. Создайте .env файл на основе примера
cp .env.example .env

# 3. Запустите приложение в Docker
docker-compose up --build

# Приложение будет доступно на http://localhost:8080
```

#### Вариант 2: Локально (без Docker)

```bash
# 1. Установите зависимости
go mod download

# 2. Создайте .env файл
cp .env.example .env

# 3. Запустите приложение
go run src/cmd/main.go

# Или соберите и запустите бинарник
go build -o bin/gotasker src/cmd/main.go
./bin/gotasker
```

### Проверка работы

После запуска откройте в браузере или используйте curl:

```bash
# Health check
curl http://localhost:8080/health

# Корневой endpoint
curl http://localhost:8080/

# API endpoint (временная заглушка)
curl http://localhost:8080/api/v1/tasks
```

Ожидаемый ответ от `/health`:
```json
{
  "status": "healthy",
  "message": "GoTasker API is running",
  "version": "0.1.0"
}
```

## Полезные команды

### Docker команды

```bash
# Запустить в фоновом режиме
docker-compose up -d

# Посмотреть логи
docker-compose logs -f

# Остановить
docker-compose down

# Пересобрать после изменений
docker-compose up --build --force-recreate

# Остановить и удалить volumes
docker-compose down -v
```

### Go команды

```bash
# Установить/обновить зависимости
go mod tidy

# Запустить приложение
go run src/cmd/main.go

# Собрать бинарник
go build -o bin/gotasker src/cmd/main.go

# Запустить тесты
go test ./...

# Запустить тесты с покрытием
go test -cover ./...

# Форматирование кода
go fmt ./...

# Проверка кода
go vet ./...
```

### Makefile команды (coming soon)

```bash
make run          # Запустить приложение локально
make build        # Собрать бинарник
make test         # Запустить тесты
make docker-up    # Запустить в Docker
make docker-down  # Остановить Docker контейнеры
```

## API Endpoints (планируется)

### Задачи (Tasks)

| Метод  | Endpoint              | Описание                    | Статус |
|--------|-----------------------|-----------------------------|--------|
| GET    | `/api/v1/tasks`       | Получить все задачи         | 🚧     |
| POST   | `/api/v1/tasks`       | Создать новую задачу        | 🚧     |
| GET    | `/api/v1/tasks/:id`   | Получить задачу по ID       | 🚧     |
| PUT    | `/api/v1/tasks/:id`   | Обновить задачу             | 🚧     |
| DELETE | `/api/v1/tasks/:id`   | Удалить задачу              | 🚧     |
| PATCH  | `/api/v1/tasks/:id/toggle` | Переключить статус Done | 🚧     |

### Примеры запросов (будут доступны после реализации)

#### Создать задачу
```bash
curl -X POST http://localhost:8080/api/v1/tasks \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Изучить Go",
    "description": "Пройти Tour of Go и сделать первый проект"
  }'
```

#### Получить все задачи
```bash
curl http://localhost:8080/api/v1/tasks
```

#### Обновить задачу
```bash
curl -X PUT http://localhost:8080/api/v1/tasks/1 \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Изучить Go - Обновлено",
    "done": true
  }'
```

## Переменные окружения

Основные переменные окружения (см. `.env.example`):

| Переменная      | Описание                     | Значение по умолчанию |
|-----------------|------------------------------|-----------------------|
| SERVER_PORT     | Порт HTTP сервера            | 8080                  |
| ENVIRONMENT     | Окружение (dev/prod)         | development           |
| DATA_FILE_PATH  | Путь к JSON файлу с данными  | data/tasks.json       |
| LOG_LEVEL       | Уровень логирования          | debug                 |

## Архитектура

Проект следует принципам **Clean Architecture**:

### Слои приложения

1. **Models** (`models/`) - Domain модели и DTO
2. **Repository** (`repository/`) - Интерфейсы для работы с данными
3. **Service** (`service/`) - Бизнес-логика
4. **Handlers** (`handlers/`) - HTTP handlers (Presentation layer)
5. **Middleware** (`middleware/`) - HTTP middleware

### Dependency Flow

```
HTTP Request → Middleware → Handlers → Service → Repository → Data Store
                                ↓
                            Response
```

### Преимущества такой архитектуры

- ✅ **Разделение ответственности** - каждый слой имеет четкую задачу
- ✅ **Тестируемость** - легко писать unit и integration тесты
- ✅ **Гибкость** - легко менять реализацию (JSON → PostgreSQL)
- ✅ **Масштабируемость** - простое добавление новых функций

## Обучающий путь

Этот проект создан для изучения Go. Подробный поэтапный план реализации смотрите в **[LEARNING_PATH.md](LEARNING_PATH.md)**.

### Основные этапы

1. ✅ **Этап 0**: Подготовка окружения
2. 🚧 **Этап 1**: Модели данных (Domain Layer)
3. 📋 **Этап 2**: Repository Interface
4. 📋 **Этап 3**: JSON Repository Implementation
5. 📋 **Этап 4**: Service Layer
6. 📋 **Этап 5**: HTTP Handlers
7. 📋 **Этап 6**: Main Application Setup
8. 📋 **Этап 7-18**: Дополнительные улучшения

**Время прохождения**: 10-50 часов в зависимости от глубины изучения.

## Разработка

### Добавление новых зависимостей

```bash
# Например, добавить библиотеку для валидации
go get github.com/go-playground/validator/v10

# Обновить go.mod и go.sum
go mod tidy
```

### Hot Reload (опционально)

Для автоматической перезагрузки при изменении файлов:

```bash
# Установить Air
go install github.com/cosmtrek/air@latest

# Запустить с hot reload
air
```

### Линтинг

```bash
# Установить golangci-lint
# https://golangci-lint.run/usage/install/

# Запустить линтер
golangci-lint run
```

## Тестирование

```bash
# Все тесты
go test ./...

# С подробным выводом
go test -v ./...

# Конкретный пакет
go test -v ./src/internal/service/...

# С покрытием
go test -cover ./...

# Генерация HTML отчета о покрытии
go test -coverprofile=coverage.out ./...
go tool cover -html=coverage.out
```

## Docker

### Multi-stage build

Dockerfile использует multi-stage сборку:
- **Builder stage**: Компиляция Go приложения
- **Runtime stage**: Минимальный Alpine образ (~15MB)

### Безопасность

- Приложение запускается от непривилегированного пользователя
- Использован минимальный базовый образ (Alpine)
- Нет лишних инструментов в production образе

## Roadmap

- [x] Базовая структура проекта
- [x] Docker конфигурация
- [ ] Модели данных (Task, CreateTaskRequest, UpdateTaskRequest)
- [ ] Repository интерфейс
- [ ] JSON Repository реализация
- [ ] Service layer
- [ ] HTTP Handlers с Gin
- [ ] Middleware (CORS, Logger, Recovery)
- [ ] Unit тесты
- [ ] PostgreSQL интеграция
- [ ] Миграции БД
- [ ] Integration тесты
- [ ] Swagger документация
- [ ] Structured logging (zap)
- [ ] Graceful shutdown
- [ ] JWT аутентификация (опционально)
- [ ] Rate limiting (опционально)
- [ ] CI/CD с GitHub Actions (опционально)

## Полезные ресурсы

### Документация Go
- [Go Tour](https://go.dev/tour/) - интерактивный тур по языку
- [Effective Go](https://go.dev/doc/effective_go) - best practices
- [Go by Example](https://gobyexample.com/) - примеры кода

### Библиотеки
- [Gin Web Framework](https://gin-gonic.com/docs/)
- [GORM ORM](https://gorm.io/docs/)
- [Testify](https://github.com/stretchr/testify) - тестирование

### Архитектура
- [Clean Architecture](https://blog.cleancoder.com/uncle-bob/2012/08/13/the-clean-architecture.html)
- [Go Project Layout](https://github.com/golang-standards/project-layout)

## FAQ

### Почему используется JSON, а не сразу PostgreSQL?

Для обучения лучше начать с простого хранилища (JSON файл), понять работу Repository Pattern, а затем мигрировать на PostgreSQL. Это демонстрирует гибкость архитектуры.

### Нужно ли знать Docker для работы с проектом?

Нет, можно запускать локально через `go run`. Но Docker упрощает деплой и изоляцию окружения.

### Как добавить новый endpoint?

1. Добавить метод в Repository интерфейс
2. Реализовать в JSON/PostgreSQL repository
3. Добавить метод в Service
4. Создать Handler
5. Зарегистрировать route в main.go

## Лицензия

MIT License - свободно используйте для обучения и коммерческих проектов.

## Автор

Учебный проект для изучения Go и backend разработки.

## Обратная связь

Если нашли ошибку или есть предложения по улучшению - создайте Issue!

---

**Happy Coding!** 🚀

Начните с изучения [LEARNING_PATH.md](LEARNING_PATH.md) и следуйте пошаговому плану!
